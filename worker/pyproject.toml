[project]
name = "testtools-backend"
version = "0.1.0"
description = "testTools backend API on Cloudflare Workers"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
